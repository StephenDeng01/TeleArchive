---
description: 完成任务后自动 git commit、push 并打版本 tag
alwaysApply: true
---

# 自动 Commit、Push 与 Tag

在本项目中，完成有意义的代码或文档变更后，**必须**自动提交并推送到远程仓库，**无需等待用户再次确认**。

## 执行时机

- 完成一项功能、修复、文档更新或配置变更后
- 用户明确要求 commit / push 时立即执行
- 不要在无变更时创建空 commit

## 版本号规则

严格遵循语义化版本（SemVer），**禁止无脑十进制递增**：

| 变更类型 | 版本升级 | 示例 |
|----------|----------|------|
| 大改 / 新架构 / 重要新功能 | minor | `0.8.0` → `0.9.0` |
| Bug 修复 / 小改 / 文档 | patch | `0.8.1` → `0.8.2` |
| 破坏性变更 | major | `0.8.x` → `1.0.0` |

每次发版前须同步更新以下三处：
1. `telearchive/__init__.py` → `__version__ = "X.Y.Z"`
2. `pyproject.toml` → `version = "X.Y.Z"`
3. tag 名称 `vX.Y.Z`

## Tag 与 Release 规则

- 每次 push 变更后，**自动在 commit 上打 patch tag**（如 `v0.8.3`），并推送到 origin。
- Tag 推送会触发 GitHub Actions 自动构建并发布 GitHub Release。
- Release 标题格式：`TeleArchive vX.Y.Z`；zip 资产命名：`TeleArchive-X.Y.Z-win64.zip`。
- **禁止** force push 已推送的 tag，如需修正须先报告错误，由用户决定。

## Git 安全协议

- **禁止**修改 git config
- **禁止** force push 到 `main` / `master`；若用户要求 force push 须先警告
- **禁止** `--no-verify`、`--no-gpg-sign` 等跳过 hook 的参数，除非用户明确要求
- **禁止**提交可能含密钥的文件（`.env`、credentials 等）
- **禁止** amend 已 push 的 commit，除非用户明确要求
- 使用 HEREDOC 编写 commit message

## 标准流程

1. 并行执行：`git status`、`git diff`、`git log -3 --oneline`
2. 确定版本号（参照上表），更新 `__init__.py` 和 `pyproject.toml`
3. 撰写 1–2 句 commit message，说明「为什么」
4. `git add` → `git commit` → `git push origin HEAD`
5. `git tag vX.Y.Z` → `git push origin vX.Y.Z`
6. `git status` 确认工作区干净、推送成功

## Commit Message 风格

与仓库现有风格一致，英文优先，例如：

- `fix: restore Qt export action and release v0.8.2`
- `feat: add check-for-update and export panel to Qt GUI`
- `docs: rewrite README for Qt-only distribution`

## 例外

- 仅探索、问答、无文件改动：不 commit，不打 tag
- push / tag push 失败时报告错误，不要 force push 修复
